pipeline {
    agent any
    stages {
        stage ('Terraform Install On Jenkins Node') {
            steps {
                sh 'echo "This is Jenkins Pipeline"'
                sh 'sudo apt-get update'
            }
        }
    }
}